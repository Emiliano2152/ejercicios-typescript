/*
 * EJERCICIO: For loop con array
 *
 * Instrucciones:
 * Escribe tu predicción ANTES de ejecutar el código.
 */

let valores = [5, 10, 15];
for (let i = 0; i < valores.length; i++) {
  console.log(valores[i]);
}

// Predicción:5 , 10 , 15 uno abajo del otro

export {};
