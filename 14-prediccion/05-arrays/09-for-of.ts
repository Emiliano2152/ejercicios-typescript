/*
 * EJERCICIO: For...of
 *
 * Instrucciones:
 * Escribe tu predicción ANTES de ejecutar el código.
 */

let colores = ['rojo', 'verde', 'azul'];
for (let color of colores) {
  console.log(color);
}

// Predicción:rojo, verde, azul uno abajo del otro

export {};
